# v0.1 Contract-Test Report

**Scope:** deterministic local tests only. This is **not** a model-quality benchmark.

The scaffold is designed to be verified with:

```bash
pytest -q
```

The committed report should be updated only from an actual test run. At scaffold creation time the local verification performed by the project generator is recorded below.

<!-- TEST_RESULT_START -->
**Result:** `10 passed in 0.10s`

Verified locally against the deterministic v0.1 scaffold. This result covers contract, grounding, abstention, safety-escalation, and mock reproducibility tests only.
<!-- TEST_RESULT_END -->

## What these tests do not establish

They do not demonstrate real-world diagnostic accuracy, calibrated confidence, retrieval quality, or maintenance-outcome improvement. Those require the later evaluation dataset and release stages.
