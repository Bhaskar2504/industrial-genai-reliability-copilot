# Evaluation datasets

The v0.2 evaluation baseline will contain synthetic cases with expected evidence, acceptable hypotheses, explicit abstention cases, and deliberately ambiguous/failing cases.

The dataset must include negative cases and must not contain proprietary plant data.
